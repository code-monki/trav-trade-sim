# Loaded automatically by Vitest (mode "test"). Value is never actually
# reached over the network — every test that exercises src/lib/api.js stubs
# global fetch — it just needs to be truthy so api.js's "not configured"
# guard doesn't short-circuit before the mocked fetch call.
VITE_API_URL=http://localhost:8787
